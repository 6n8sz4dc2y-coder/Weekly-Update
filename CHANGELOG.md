# Changelog

## v2.0.1
- Added `data.js` as the permanent shared data source.
- Dashboard now loads `data.js` before `app.js`.
- Admin Publish still updates the local preview.
- Admin Download now creates a ready-to-upload `data.js` file for GitHub.
- Keeps root-file structure only.

## v2.0
- Added trends framework and current dashboard layout.
