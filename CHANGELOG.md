# Changelog

## v2.0.1
- Added permanent `data.js` source file.
- Dashboard now loads shared data from `data.js` before `app.js`.
- Local browser data is only used if no `data.js` is present.
- Root files only.
